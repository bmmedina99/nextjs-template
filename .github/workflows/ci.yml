name: CI Pipeline

on:
  push:
    branches: [production] # Cambiar a la rama principal
  pull_request:

permissions:
  contents: read

env:
  NODE_VERSION: 24

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-check:
    name: Calidad y linting
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4

      - name: Usando Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Instando dependencias
        run: pnpm install --frozen-lockfile

      - name: Revisando linting
        run: pnpm run lint

      - name: Revisando formato de código
        run: pnpm run fmt
        continue-on-error: true

      - name: Realizando la build
        run: pnpm run build --if-present
        if: always()

  security-check:
    name: Securidad y actualizaciones
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4

      - name: Usando Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Instando dependencias
        run: pnpm install --frozen-lockfile

      - name: Revisando vulnerabilidades
        run: pnpm audit --audit-level=moderate
        continue-on-error: true

      - name: Revisando dependencias desactualizadas
        run: pnpm outdated
        continue-on-error: true
